# تغيير منفذ قاعدة البيانات إلى 3308

## ✅ ما تم تحديثه:

1. **`config.py`** - إضافة `DB_PORT` مع القيمة الافتراضية 3308
2. **`.env`** - إضافة `DB_PORT=3308`
3. **`setup_database.sh`** - تحديث لاستخدام المنفذ 3308
4. **`test_db_connection.py`** - تحديث لعرض المنفذ

## ⚙️ إعداد MySQL للعمل على المنفذ 3308

### الطريقة 1: استخدام ملف إعداد MySQL

قم بإنشاء أو تعديل ملف إعداد MySQL:

**على macOS (Homebrew):**
```bash
# ملف الإعداد عادة في:
/usr/local/etc/my.cnf
# أو
/opt/homebrew/etc/my.cnf
```

أضف أو عدّل:
```ini
[mysqld]
port = 3308
```

**على Linux:**
```bash
# ملف الإعداد عادة في:
/etc/mysql/my.cnf
# أو
/etc/my.cnf
```

أضف:
```ini
[mysqld]
port = 3308
```

### الطريقة 2: تشغيل MySQL على منفذ مخصص

```bash
# تشغيل MySQL على المنفذ 3308
mysqld --port=3308
```

### الطريقة 3: استخدام Docker (إذا كنت تستخدم Docker)

```yaml
# docker-compose.yml
services:
  mysql:
    image: mysql:8.0
    ports:
      - "3308:3306"
    environment:
      MYSQL_ROOT_PASSWORD: your_password
```

## 🔄 إعادة تشغيل MySQL

بعد تعديل الإعدادات:

**على macOS (Homebrew):**
```bash
brew services restart mysql
```

**على Linux:**
```bash
sudo systemctl restart mysql
# أو
sudo service mysql restart
```

## ✅ اختبار الاتصال

### 1. اختبار الاتصال من Terminal:

```bash
mysql -u root -p -P 3308 -h localhost
```

### 2. اختبار من Python:

```bash
cd backend
python test_db_connection.py
```

يجب أن يظهر:
```
📋 إعدادات قاعدة البيانات:
   Host: localhost
   Port: 3308
   User: root
   Database: corrosion_db
```

### 3. اختبار من Backend:

```bash
cd backend
python app.py
```

راقب السجلات للتأكد من الاتصال الناجح.

## 📝 ملاحظات مهمة

1. **إذا كان MySQL يعمل على المنفذ 3306 (الافتراضي):**
   - يجب تغيير إعدادات MySQL للعمل على 3308
   - أو استخدام منفذ آخر متاح

2. **التحقق من المنفذ المستخدم:**
   ```bash
   # على macOS
   lsof -i :3308
   
   # على Linux
   netstat -tuln | grep 3308
   # أو
   ss -tuln | grep 3308
   ```

3. **إذا كان المنفذ 3308 مستخدم:**
   - اختر منفذ آخر (مثل 3307, 3309)
   - أو أوقف العملية التي تستخدمه

## 🔧 استكشاف الأخطاء

### خطأ: "Can't connect to MySQL server on 'localhost' (61)"

**الحل:**
- تأكد من أن MySQL يعمل على المنفذ 3308
- تحقق من إعدادات MySQL

### خطأ: "Access denied"

**الحل:**
- تحقق من كلمة المرور في `.env`
- تأكد من صلاحيات المستخدم

### خطأ: "Unknown database"

**الحل:**
```bash
cd backend
./setup_database.sh
```

---

**تم التحديث بنجاح! 🎉**

الآن Backend سيحاول الاتصال بـ MySQL على المنفذ **3308**.

